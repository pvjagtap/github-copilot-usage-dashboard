{
  "name": "copilot-usage-dashboard",
  "displayName": "Copilot Usage Dashboard",
  "description": "Live token usage dashboard for GitHub Copilot — prompt, output, and cached tokens with full analytics.",
  "version": "1.10.14",
  "publisher": "pvjagtap",
  "icon": "images/icon.png",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/pvjagtap/github-copilot-usage-dashboard"
  },
  "homepage": "https://github.com/pvjagtap/github-copilot-usage-dashboard#readme",
  "bugs": {
    "url": "https://github.com/pvjagtap/github-copilot-usage-dashboard/issues"
  },
  "keywords": [
    "copilot",
    "token",
    "usage",
    "dashboard",
    "analytics",
    "opentelemetry",
    "github"
  ],
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other",
    "Visualization"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "copilotUsage.openDashboard",
        "title": "Copilot Usage: Open Dashboard"
      },
      {
        "command": "copilotUsage.refresh",
        "title": "Copilot Usage: Refresh Stats"
      },
      {
        "command": "copilotUsage.dailyLimit.snooze",
        "title": "Copilot Usage: Snooze Daily Limit Alert"
      },
      {
        "command": "copilotUsage.dailyLimit.resume",
        "title": "Copilot Usage: Resume Copilot (Override Daily Limit)"
      },
      {
        "command": "copilotUsage.dailyLimit.reset",
        "title": "Copilot Usage: Reset Today's Daily-Limit Counter"
      },
      {
        "command": "copilotUsage.dailyLimit.showShield",
        "title": "Copilot Usage: Show Daily Limit Shield"
      },
      {
        "command": "copilotUsage.dailyLimit.installHooks",
        "title": "Copilot Usage: Install Agent Hooks (CLI / Custom Agents / Cloud)"
      },
      {
        "command": "copilotUsage.dailyLimit.uninstallHooks",
        "title": "Copilot Usage: Uninstall Agent Hooks"
      },
      {
        "command": "copilotUsage.aic.detectPlan",
        "title": "Copilot Usage: Detect My Copilot Plan"
      },
      {
        "command": "copilotUsage.sidebar.refresh",
        "title": "Refresh",
        "category": "Copilot Usage",
        "icon": "$(refresh)"
      },
      {
        "command": "copilotUsage.sidebar.openDashboard",
        "title": "Open Full Dashboard",
        "category": "Copilot Usage",
        "icon": "$(link-external)"
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "copilotUsage",
          "title": "Copilot Usage",
          "icon": "images/sidebar-icon.svg"
        }
      ]
    },
    "views": {
      "copilotUsage": [
        {
          "id": "copilotUsage.panel",
          "name": "Usage",
          "type": "webview",
          "icon": "images/sidebar-icon.svg",
          "contextualTitle": "Copilot Usage"
        }
      ]
    },
    "menus": {
      "view/title": [
        {
          "command": "copilotUsage.sidebar.refresh",
          "when": "view == copilotUsage.panel",
          "group": "navigation@1"
        },
        {
          "command": "copilotUsage.sidebar.openDashboard",
          "when": "view == copilotUsage.panel",
          "group": "navigation@2"
        }
      ]
    },
    "configuration": [
      {
        "title": "Copilot Usage — General",
        "properties": {
          "copilotUsage.workspaceStoragePath": {
            "type": "string",
            "default": "",
            "description": "Optional absolute path to a VS Code workspaceStorage directory. When empty, the extension auto-detects across Linux (~/.config/Code), macOS (~/Library/Application Support/Code), Windows (%APPDATA%/Code), dev containers / Remote-SSH / WSL (~/.vscode-server), and VS Code Insiders. Set this if you run a portable install, a fork, or need to point at a specific install."
          },
          "copilotUsage.cli.enabled": {
            "type": "boolean",
            "default": true,
            "description": "Include GitHub Copilot CLI (@github/copilot) session usage in the dashboard. When on, the extension scans ~/.copilot/session-state/ (overridable below) for live prompt activity and session.shutdown billing ledgers. Disable to exclude CLI usage entirely."
          },
          "copilotUsage.cli.homePath": {
            "type": "string",
            "default": "",
            "description": "Optional absolute path to the GitHub Copilot CLI home directory (where session-state/ lives). When empty, resolves to the COPILOT_HOME env var, then falls back to ~/.copilot. Set this if you run the CLI from a portable install or a non-standard location."
          }
        }
      },
      {
        "title": "Copilot Usage — AI Credits (AIC)",
        "properties": {
          "copilotUsage.aic.plan": {
            "type": "string",
            "default": "business",
            "enum": [
              "free",
              "pro",
              "pro_plus",
              "max",
              "business",
              "business_promo",
              "enterprise",
              "enterprise_promo"
            ],
            "enumDescriptions": [
              "Free — 2,000 completions/month (no published credit allowance)",
              "Pro — $10/month (1,500 credits: 1,000 base + 500 flex)",
              "Pro+ — $39/month (7,000 credits: 3,900 base + 3,100 flex)",
              "Max — $100/month (20,000 credits: 10,000 base + 10,000 flex)",
              "Business — $19/user/month (1,900 credits/user, pooled). Promo applies to business_promo.",
              "Business Promo — June–Sept 2026 (3,000 credits/user, pooled). Existing Business customers only.",
              "Enterprise — $39/user/month (3,900 credits/user, pooled). Promo applies to enterprise_promo.",
              "Enterprise Promo — June–Sept 2026 (7,000 credits/user, pooled). Existing Enterprise customers only."
            ],
            "description": "Your GitHub Copilot plan. Determines included monthly AI credits. Promotional uplift (June–Sept 2026) applies only to existing Business/Enterprise customers, not to Free/Pro/Pro+/Max."
          },
          "copilotUsage.aic.autoDetectPlan": {
            "type": "boolean",
            "default": true,
            "description": "On first activation, silently detect your GitHub Copilot plan via your existing GitHub session (no extra sign-in required) and write it to copilotUsage.aic.plan. Falls back to a one-time picker if detection fails. Set false to keep the manually configured plan only."
          },
          "copilotUsage.aic.billingCycleStartDay": {
            "type": "number",
            "default": 1,
            "minimum": 1,
            "maximum": 28,
            "description": "Day of month when your billing cycle starts (1-28)."
          },
          "copilotUsage.aic.monthlyCreditsIncluded": {
            "type": "number",
            "default": 1900,
            "description": "Override monthly included credits per user (set 0 for unlimited). Defaults based on plan selection."
          },
          "copilotUsage.aic.overageCostPerCredit": {
            "type": "number",
            "default": 0.01,
            "description": "Cost in USD per AI credit overage. Official rate: 1 credit = $0.01 USD."
          },
          "copilotUsage.aic.customModelCosts": {
            "type": "array",
            "default": [],
            "description": "Custom per-model credit rates (per 1M tokens). Overrides defaults for specified models.",
            "items": {
              "type": "object",
              "properties": {
                "model": {
                  "type": "string",
                  "description": "Model identifier (e.g., 'claude-opus-4.6')"
                },
                "inputCreditsPerMillion": {
                  "type": "number",
                  "description": "Credits per 1M input tokens"
                },
                "outputCreditsPerMillion": {
                  "type": "number",
                  "description": "Credits per 1M output tokens"
                },
                "cachedInputCreditsPerMillion": {
                  "type": "number",
                  "description": "Credits per 1M cached input tokens (read)"
                },
                "cacheWriteCreditsPerMillion": {
                  "type": "number",
                  "description": "Credits per 1M cache write tokens (Anthropic only, 0 for others)"
                },
                "tier": {
                  "type": "string",
                  "enum": [
                    "base",
                    "premium",
                    "custom"
                  ],
                  "description": "Cost tier"
                }
              },
              "required": [
                "model",
                "inputCreditsPerMillion",
                "outputCreditsPerMillion",
                "cachedInputCreditsPerMillion",
                "cacheWriteCreditsPerMillion",
                "tier"
              ]
            }
          },
          "copilotUsage.aic.includeOnlyBilledModels": {
            "type": "boolean",
            "default": true,
            "description": "When true (default), only models that GitHub Copilot actually bills in AI Credits (Claude/GPT/Gemini families in the rate table) count toward the headline total, budget bar, and projected spend. Local Ollama, LM Studio, BYOK keys, and unrecognised models are still tracked and shown in a separate 'Non-billable models (informational)' panel for capacity planning, but excluded from billed-cost math. Disable to restore the legacy behaviour where every chat call inflates AIC totals. (Issue #5)"
          },
          "copilotUsage.aic.excludeModels": {
            "type": "array",
            "default": [],
            "description": "Model name substrings to force-EXCLUDE from billable totals, even if they otherwise match the GHC rate table. Use this to mark a specific alias (e.g. 'raptor-mini-internal') as informational. Matching is case-insensitive substring. (Issue #5)",
            "items": { "type": "string" }
          },
          "copilotUsage.aic.extraBilledModels": {
            "type": "array",
            "default": [],
            "description": "Model name substrings to force-INCLUDE as billable even when they don't appear in the built-in rate table. Escape hatch for preview models that arrive before the table is updated. Matching is case-insensitive substring. (Issue #5)",
            "items": { "type": "string" }
          },
          "copilotUsage.aic.useOnlineModelCatalog": {
            "type": "boolean",
            "default": true,
            "description": "When true (default), fetch the authoritative GitHub Copilot model catalog once per 24h (CDN known-models manifest + Copilot CAPI /models) and use it as the primary billable-vs-non-billable classifier. The CDN manifest is the same URL the official Copilot Chat extension reads on startup, and the CAPI response includes the canonical billing.multiplier field. Lets the dashboard recognise new preview models (e.g. Claude Fable 5) without an extension update, and correctly excludes BYOK model IDs that happen to substring-match a known family. Disable to operate fully offline using only the built-in rate table. (Issue #5)"
          }
        }
      },
      {
        "title": "Copilot Usage — Daily Limit Guard",
        "properties": {
          "copilotUsage.dailyLimit.enabled": {
            "type": "boolean",
            "default": true,
            "description": "Enable the daily AI Credits limit guard with visual indicator + optional Copilot pause."
          },
          "copilotUsage.dailyLimit.credits": {
            "type": "number",
            "default": 100,
            "minimum": 1,
            "description": "Maximum AI Credits (AIC) you allow yourself to spend per day. 1 AIC ≈ $0.01."
          },
          "copilotUsage.dailyLimit.dollars": {
            "type": "number",
            "default": 0,
            "minimum": 0,
            "description": "Optional: set a daily dollar limit instead of credits. If > 0, this overrides 'credits' (converted via copilotUsage.aic.overageCostPerCredit, default $0.01/credit). Set to 0 to use the credits-based limit."
          },
          "copilotUsage.dailyLimit.warnAtPercent": {
            "type": "number",
            "default": 75,
            "minimum": 10,
            "maximum": 99,
            "description": "Percent of daily limit at which the first warning appears (status bar turns amber)."
          },
          "copilotUsage.dailyLimit.braceAtPercent": {
            "type": "number",
            "default": 90,
            "minimum": 50,
            "maximum": 99,
            "description": "Percent of daily limit at which the floating corner card appears (orange)."
          },
          "copilotUsage.dailyLimit.resetHour": {
            "type": "number",
            "default": 0,
            "minimum": 0,
            "maximum": 23,
            "description": "Local hour at which the daily counter resets (0 = midnight)."
          },
          "copilotUsage.dailyLimit.enforcement": {
            "type": "string",
            "default": "pause",
            "enum": [
              "soft",
              "pause",
              "strict"
            ],
            "enumDescriptions": [
              "Soft — Show overlay + nag modal only. Copilot keeps working.",
              "Pause — Turn off Copilot completions and show modal block for chat (no reload).",
              "Strict — Disable Copilot + Copilot Chat extensions (requires window reload)."
            ],
            "description": "What to do when the daily limit is reached."
          },
          "copilotUsage.dailyLimit.snoozeMinutes": {
            "type": "number",
            "default": 10,
            "minimum": 1,
            "maximum": 240,
            "description": "How long the 'Snooze' button silences the limit overlay."
          },
          "copilotUsage.dailyLimit.playSound": {
            "type": "boolean",
            "default": false,
            "description": "Play a soft chime when the daily limit is first reached."
          },
          "copilotUsage.dailyLimit.installAgentHooks": {
            "type": "boolean",
            "default": true,
            "description": "Install global GitHub Copilot lifecycle hooks (~/.copilot/hooks) so the daily limit also denies tool calls in Copilot CLI, local custom agents, and the cloud agent. Does not affect plain Copilot Chat Ask mode (use 'strict' enforcement for that). Disable to remove hooks."
          }
        }
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.85.0",
    "@typescript-eslint/eslint-plugin": "^8.58.1",
    "@typescript-eslint/parser": "^8.58.1",
    "@vscode/vsce": "^3.9.1",
    "eslint": "^9.39.4",
    "typescript": "^5.3.0"
  }
}
